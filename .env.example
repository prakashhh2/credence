# Credence - Solana Certificate Issuance & Verification App
# Environment Configuration

# ==========================================
# SOLANA DEVNET CONFIGURATION
# ==========================================

# Solana RPC Endpoint (Devnet)
# Leave blank to use default devnet RPC
# REACT_APP_SOLANA_RPC_URL=https://api.devnet.solana.com

# ==========================================
# STORAGE CONFIGURATION
# ==========================================

# For production, configure IPFS/Arweave storage
# Currently using stub implementation (localStorage + data URLs for demo)
# 
# Example for Pinata IPFS:
# REACT_APP_PINATA_API_KEY=your_api_key
# REACT_APP_PINATA_SECRET_KEY=your_secret_key
#
# Example for Arweave:
# REACT_APP_ARWEAVE_ENDPOINT=https://arweave.net
# REACT_APP_BUNDLR_ENDPOINT=https://node1.bundlr.network

# ==========================================
# FRONTEND CONFIGURATION
# ==========================================

# Frontend URL for QR code generation (optional)
# REACT_APP_FRONTEND_URL=http://localhost:3000

# ==========================================
# DEVELOPMENT
# ==========================================

# Enable debug logging
REACT_APP_DEBUG=true

  - Chain ID: 80001
  - Currency: MATIC
  - Block Explorer: https://mumbai.polygonscan.com

### 2. Get Test MATIC Tokens

- Visit [Polygon Faucet](https://faucet.polygon.technology/)
- Enter your wallet address
- Request test MATIC (needed for gas fees)

### 3. Setup Pinata IPFS

- Create account at [Pinata.cloud](https://www.pinata.cloud)
- Go to API Keys
- Create new API key
- Copy API Key and Secret Key
- Add to `.env.local`

### 4. Deploy Smart Contract

Use Hardhat or Remix to deploy `CredenceCertificate.sol`:

#### Option A: Using Remix (Easiest)
1. Go to [remix.ethereum.org](https://remix.ethereum.org)
2. Create new file: `CredenceCertificate.sol`
3. Copy contract code from `contracts/CredenceCertificate.sol`
4. Select Compiler version 0.8.0+
5. Compile
6. Deploy to Polygon Mumbai
7. Copy contract address to `.env.local`

#### Option B: Using Hardhat
```bash
npm install --save-dev hardhat @nomicfoundation/hardhat-toolbox
npx hardhat init
# Follow prompts, then deploy
npx hardhat run scripts/deploy.js --network mumbai
```

### 5. Authorize Universities

After deployment, authorize university addresses:
```javascript
// In contract owner's address:
await contract.authorizeIssuer("0x...university_address...")
```

## Web3 Integration Files

- **web3Service.js** - Wallet connection, contract interaction
- **ipfsService.js** - File uploads to IPFS via Pinata
- **transactionTracker.js** - Local transaction history management

## Testing

### Test Certificate Upload Flow:
1. Connect wallet (MetaMask)
2. Switch to Polygon Mumbai network
3. Upload certificate (will be stored on IPFS)
4. Certificate hash stored on blockchain
5. Student can claim certificate with their address
6. Certificate is verifiable on-chain

### Verify Transaction:
- Check transaction on [Mumbai Polygonscan](https://mumbai.polygonscan.com)
- View IPFS file on [Pinata Gateway](https://gateway.pinata.cloud)

## Costs (Testnet)

- **Gas Fees**: ~0.01 MATIC per transaction (free testnet tokens)
- **IPFS Storage**: 1GB free on Pinata (paid plans available)

## Mainnet Deployment

For production on Polygon Mainnet:
1. Change RPC to: `https://polygon-rpc.com`
2. Get actual MATIC from exchange
3. Update contract address for mainnet
4. Update environment variables
